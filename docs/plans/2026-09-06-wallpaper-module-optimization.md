# 寻找壁纸模块优化计划

日期：2026-09-06；最新实施记录：2026-09-07

状态：实施中，按当前工作树重新排期。P0-A、P0-B 已有主要实现，待补完整实机验收；P1-A、P1-B 部分实现且已有回归通过记录；P2-A 的媒体目录、收藏和用途筛选已实现首批，文件管理待补；P2-B 待实施；P2-C 已有能力核实记录，待落实；最终 UI/UX 尚未验收。完整目标不变。

本次修订按用户要求制定下一步优化计划，更新计划及 QA 记录，不修改产品代码。代码现状与可执行文件时间经过本次只读核对；测试和实际 UI 操作结果来自上一实施阶段的交接记录，本次未重复执行。此前通过的检查不代表其后的全部改动通过。

2026-09-07 实施补记：已落实相册临时加载/页面变化的区分、展开页恢复、库缓存过期后的分页重取及统一库工具栏；相册恢复新增集成用例通过。生成返回持久元数据及图片实际尺寸；原图物化保存来源与许可；每批最多 96 项的媒体查询恢复远程结果收藏及本地路径，覆盖迟到结果隔离。Rust 壁纸回归 190 项通过，最新构建和实机证据持续写入 QA。详情、提示词复用、文件管理、批量操作、完整筛选/性能和全模块 UI 验收仍未完成。

最新计划核对：已核对磁盘上的调试可执行文件为 2026-09-07 08:53:41、77692928 字节。交接记录确认该构建包含原图最多两个在途请求、只保留最新排队项的限制，以及搜索框高度修复；相关前端 31 项回归通过。该版本已有 1143×793 窗口布局和 `result.jpg` 四项搜索结果进入改图再返回的实机记录，但不能代替翻页后滚动位置、最小窗口和两入口验收。本次没有重跑测试或操作应用。

当前工作树另有未完成的详情弹窗、作者来源持久化和提示词复用改动，晚于上述构建。详情组件引用的新翻译键、样式、父图导航和针对性测试尚未补齐；不得把旧构建或历史 193 项 Rust 回归视为这些新增改动的验证。下一批先收尾这组改动，再验收浏览与生成恢复，避免继续累积未验证功能。

## 目标与范围

围绕“找到图片 → 对比预览 → 收藏或生成 → 再次找到 → 应用背景”的完整流程优化。
覆盖 X、网络、Openverse、Pexels、Imagine、Grok 相册、本地壁纸库七个来源，以及普通生图、改图和图生视频。
这里的“应用壁纸”沿用现有应用背景功能，不扩展为 Windows 系统桌面壁纸管理。

优先顺序：结果可靠性 → 图库完整性 → 浏览连续性 → 失败恢复 → 收藏和筛选 → 视频能力。
沿用现有来源导航、卡片、预览器及媒体管线，不重做整套界面。

## 当前基线

| 项目 | 当前状态与证据边界 |
| --- | --- |
| 七个来源、原比例显示、图片和视频预览、应用背景 | 已有实现；之前的设备验收见 `docs/qa/2026-09-05-wallpaper-video-hardening.md`，不是本轮重新实测 |
| 各来源图片进入改图或生成视频、本地图片上传 | 已有实现，保留当前未提交工作 |
| 改图 16:9 | 上一轮新版应用内输出 1280×720、无白边，相关 Rust 测试 18 项通过；其他比例不可宣称已逐项真实生成验收 |
| 普通生图 P0-A | 已有独立请求/输出目录、单次工具调用审计及取消实现，旧的当天目录回填逻辑已移除；正常生成至预览、入库、应用的新版真实流程待验收 |
| 本地库 P0-B | 已有 Host 快照分页、默认 48 项、完整集合的文本/类型/用途筛选，搜索包括已记录标题和提示词；已有 251 项 fixture。02:30 构建已验证 48 → 96 → 111 项追加；08:34 构建从初始页搜索末项视频成功，预览可见画面变化，关闭后查询与结果保留。此项不再列为未验证 |
| 来源切换 P1-A | 已有来源历史、本地库查询/页缓存、完成预取恢复、迟到响应隔离和凭证变更失效；前端缓存 20 分钟、每查询最多 2000 项、本地库最多 8 个查询。相册临时加载与页面变化已区分，展开页恢复、过期快照重取已有回归；跨来源往返和账号变化仍需实机验收 |
| 失败处理 P1-B | 原图失败保留缩略图、手动重试、迟到结果隔离、尺寸读取 20 秒超时及生成错误分类已有实现。08:53 构建包含两请求并发上限和最新项排队，前端回归覆盖失败释放、关闭重开和卸载；仍缺真实原图失败恢复、普通生成及取消验收。429 不直接解释为额度耗尽 |
| 收藏与媒体记录 P2-A | 持久目录、稳定 ID、图片尺寸、生成参数、父图关联及收藏已有实现；远程结果可恢复已记录的收藏。详情、作者持久化、提示词复用是当前未完成代码，尚未随新版构建验证；父图导航、七来源回显和元数据写入失败恢复仍待完成 |
| 工具栏与布局 | 08:53 构建已有控件等高及 1143×793 窗口实机记录，图片保持原比例；首屏过滤结果进入改图返回保留查询和四项结果。加载时弹窗高度/位置变化、窄窗口源图移除按钮另起一行仍待处理；最小窗口、两入口、主题和缩放矩阵尚未验收 |
| 文件删除 | `library_delete` 实际调用 `fs::remove_file`，当前不是回收站或可撤销删除 |
| 视频选项 P2-C | 当前只有 6/10 秒、480p/720p；交接核实的订阅工具未提供图生视频比例/模型参数及 1080p；不能按官方收费 API 的能力直接补下拉框 |

检查记录边界：本次只读核对磁盘构建时间、上述相关源码、能力报告和未提交工作树；未确认当前运行进程。测试和 UI 结果均注明为交接证据，范围可能重叠，不相加作为总覆盖率。本次更新计划和 QA，不以文档更新代替功能交付。详见 `docs/qa/2026-09-06-wallpaper-module-optimization.md`。

## 下一步与依赖

| 批次 | 当前要做的工作 | 交付与验收 |
| --- | --- | --- |
| 0. 收尾当前半成品 | 完成详情、作者持久化和提示词复用；补 15 语言、样式、旧目录兼容、焦点/关闭行为及针对性回归；接通父图导航 | 当前树类型检查与相关前端/Rust 检查通过，构建后详情可用；复用只预填普通生图，不触发生成或覆盖改图/视频草稿；父图丢失可解释 |
| 1. 浏览和失败恢复 | 实测原图失败恢复、相册与缓存恢复及生成错误处理；修复加载导致弹窗跳动和窄窗口源图操作布局 | 翻完 111 项、全库搜索末项、并发限制和首屏改图往返已有证据；补搜索翻页后的滚动恢复、账号隔离、普通生图与取消、两个入口 |
| 2. 收藏与作品闭环 | 验收来源记录、即时元数据、远程收藏回显和批次 0 详情；处理历史映射缺失、收藏更新竞态和元数据失败恢复 | 七来源回显不串项；刚生成与库内同一作品信息一致；历史未知信息保持未知；元数据保存失败可以找回已生成文件而不必重新生成 |
| 3. 单项文件管理 | 先完成可恢复删除与恢复入口，再接另存为、打开文件夹；处理恢复同名冲突、当前背景保护和目录记录一致性 | 删除后可恢复；另存为和恢复不静默覆盖；当前背景不因文件操作失效 |
| 4. 批量管理与清理 | 补多选、批量收藏/导出/删除及逐项结果；清理前列出范围和大小，保护收藏、作品、当前背景和任务源文件 | 部分失败可定位重试；清理预览与执行范围一致；被保护文件不会被误删 |
| 5. 筛选、质量和性能 | 增加方向、比例、最低分辨率、来源筛选；补确定性去重；固定主题比较相关率/重复率；测量 500 项库后处理实际瓶颈 | 全库筛选与分页一致；远程本地筛选范围清楚；未知尺寸单列；提供同设备同样本的性能和搜索质量前后记录 |
| 6. 生成能力 | 复核安装版工具契约；分别列出生图/改图/视频支持参数；落实可支持选项及结果尺寸、时长校验 | 模型、比例、1080p 必须有当前渠道支持证据才能开放；需要先改图再生视频时明确两步生成；收费 API 与订阅额度分开说明 |
| 7. 全模块 UI/UX | 统一工具栏、卡片操作、预览详情、生成参数和批量操作；完善窄窗口、焦点、加载与错误状态 | 七来源、两个入口、明暗主题和不同窗口尺寸通过 P3 矩阵；完成找图到应用背景的整条流程 |

批次 0 → 1 → 2 收尾后开展文件管理；单项删除/恢复验收通过后才复用到批量与清理。批次 5 依赖媒体记录，批次 6 的只读核实已有首批报告，安装版契约与实际输出仍需验证。各批次同步落实对应 UI，批次 7 负责统一收尾，不能用最终美化替代前面各项功能验收。

下一轮从批次 0 开始，按以下顺序执行：

1. 收尾详情弹窗及 15 语言，复用已有格式化方法展示大小和时长；明确筛选移除当前项时关闭详情，Escape 只关闭顶层弹窗且焦点回到入口。
2. 验证作者信息持久化和旧目录兼容；提示词复用覆盖忙碌保护、比例回填及草稿隔离；父图按稳定 ID 导航，缺失时不跳错图。完成相关检查后再构建并验收新增代码。
3. 保留已完成的并发限制与控件等高修复；补原图失败恢复、搜索翻页后改图返回、来源与账号切换的实机覆盖，同时修复弹窗跳动和源图操作换行。
4. 完成最少量普通生图、取消、入库和应用背景验收；七来源完成元数据/收藏回显。写入失败应能重试保存或找回文件，不能要求重新生成。之后开展单项文件管理。

每批产物为可运行版本、对应 QA 记录及剩余项。涉及外部渠道的能力结论按核实日期记录；不能将未知支持能力写成承诺。当前排期按依赖和验收推进，不在未取得性能基线或上游支持证据前给出虚假的完成时间。

已知边界纳入对应批次：作者持久化新增代码尚未验证；历史文件缺少来源 URL 映射时不能自动回显收藏；带签名 URL 的变化不能直接认作同一文件；视频尚未探测实际尺寸。内容去重在批次 5 处理，不通过任意去掉 URL 查询参数合并资源。500 项测量再决定是否优化目录整表复制或画廊渲染，不预先扩大重构范围。

首个 UI 调整已实现：把壁纸库分散的刷新按钮、用途选择和类型/搜索筛选合并为一组响应式工具栏；刷新使用现有图标，保留提示和忙碌状态。宽窗口紧凑排列，窄窗口按组换行，避免固定占用三行挤压图片。实际新版窗口验收独立记录在 QA 中。

顺序：M1 → M2 → M3 → M5。M4 的只读核实与前几阶段并行推进；当前渠道支持的改动纳入 M5 验收，新收费渠道保留为单独决策，不阻塞其他里程碑。

主要代码入口：`src/components/WallpaperSourceModal.tsx`、`WallpaperSourceGallery.tsx`、`WallpaperImagineControls.tsx`，`src/hooks/useWallpaper*`，`src/lib/wallpaper*`，`src-tauri/src/wallpaper_source.rs` 与 `wallpaper_imagine_video/`。

## 执行阶段

### P0-A：普通生图可靠性与取消

- 每次任务使用独立请求 ID、会话和输出目录，彻底移除扫描当天目录回填成功结果的行为。
- 复用已存在的受限工具执行、取消注册、会话审计与 Host 复制结果模式；根据实际 `image_gen` 参数契约实现验收。
- 默认一次生成一张，生成数量由明确参数决定，不允许模型自行追加变体或自动重试。
- 只接收本次会话生成且通过路径、签名、大小校验的图片；元数据记录用户原始提示词及实际可确认的参数。
- 取消、关闭窗口和离开生成来源终止本次后台任务，清理临时资源，迟到结果不能覆盖当前界面。
- 保持改图和图生视频已验证的行为；共享代码只在实际消除重复时抽取。

验收：当天已有图而本次返回无效 JSON/空结果时必须报错；旧文件、会话外路径和非图片不能成功入库；取消后无迟到结果；正常结果可预览、重新找到并应用。先用确定性 fixture 覆盖失败路径，再做最少量真实生成验收。

### P0-B：完整图库与稳定分页

- 增加 Host 分页接口，返回稳定排序的条目、下一页游标和可确认的计数；保留旧接口以兼容其他调用者。
- 排序在 Host 完成，明确静图优先、修改时间和稳定 ID 的顺序，避免每页在前端重新排序导致跳动。
- 库内搜索和类型筛选作用于完整集合；切换条件重置分页，不能仅过滤已加载的 96 项。
- 明确扫描快照和刷新语义：加载过程中新增文件下次刷新出现；删除和时间相同的文件不造成重复或漏页。无效游标明确失败。
- 首先复用现有扫描和缓存设施；根据实际图库规模再确定是否需要持久索引，避免直接引入新数据库。

验收：使用至少 250 个混合媒体 fixture，所有文件可翻页访问；能搜索到第 96 项之外的文件；同时间文件、新增、删除、刷新均不重复漏项；同一视图追加时已有卡片位置稳定。

### P1-A：保留各来源浏览状态

- 把来源工作状态移入领域 hook，按来源保留查询、排序、结果、选中项、筛选、分页和滚动位置。
- 本阶段保证同一次弹窗打开期间的返回恢复；重新启动后的完整搜索历史不混入首轮交付。
- 从搜索图进入改图或视频后，可以回到原来源继续浏览；三个生成模式的提示词继续独立保存。
- 来源切换停止不再需要的网络工作；正在生成的任务继续遵守 P0-A 的取消语义，不引入后台任务队列。
- 缓存有条数和生命周期上限；账号、Pexels 凭证及来源身份变化时失效，不能恢复前一身份的私有相册数据。

验收：搜索并翻页 → 滚动选图 → 进入改图 → 返回，结果、位置和分页恢复；切换来源不额外发起重复搜索；迟到响应不能写入其他来源。

### P1-B：失败可见、可重试

- 为卡片区分缩略图失败、原图失败、加载中和成功；网络失败保留条目及原有尺寸。
- 单项重试只重新请求失败媒体，限制并发；原图失败时仍保留可用缩略图。
- 生成错误使用稳定分类，覆盖登录、额度/限流、超时、格式、上游失败、结果校验失败；无法确定原因时诚实显示未知错误。
- 生成重试由用户主动触发新的任务，不自动重复消耗额度；保留源图、提示词和选项。
- 错误文案给出对应可执行动作；不直接显示原始 CLI 日志或认证内容。

验收：模拟离线、403、429、超时、解码失败；已有卡片不消失，成功项不重载，重试后可恢复；关闭或换来源后重试结果失效。覆盖主设置窗口与独立主题编辑器。

### P2-A：收藏、元数据和文件管理

- 引入最小媒体目录记录：稳定 ID、来源、来源页/许可、已知尺寸、用户提示词、生成参数、父图关联和收藏状态。
- 为旧文件渐进补录，无法追溯的数据保持未知；兼容已有目录和文件，不要求搬迁或重新下载。
- “浏览缓存”“已收藏”“生成作品”分别记录用途。提供收藏/取消收藏和按用途查看；取消收藏不删除原文件。
- 提供单项另存为、打开所在文件夹；再补批量收藏、导出和删除，逐项报告成功与失败。
- 区分删除条目和删除文件；采用现有可用回收设施或应用内可恢复删除，定义恢复期限及同名冲突处理。
- 清理缓存先展示范围和大小，保护收藏、生成作品、当前背景及任务正在使用的源文件；不自动清理旧数据。

验收：重启后收藏和提示词仍在；生成结果可追溯源图并复用提示词；旧库正常显示；另存为不静默覆盖；批量部分失败不宣称全部成功；删除可恢复且不影响当前背景。

### P2-B：找图效率与质量

- 首批增加横图/竖图/方图、比例、最低分辨率和来源筛选，以及尺寸和文件信息查看；未知尺寸单列，不伪造满足条件。
- 远程来源仅把其真实支持的过滤参数发给上游；本地过滤标明只针对当前已加载结果，避免把筛空误报为上游无结果。
- 库内过滤走完整集合；搜索词与筛选条件共同参与缓存和分页身份。
- 先补规范 URL、文件内容的确定性去重；视觉近似去重作为后续独立项，用固定样本评估后启用，不直接删除相似原文件。
- 用固定主题集评估网络结果相关性，记录相关率、重复率、有效数量和耗时，再调整查询或排序。
- 性能先测量再优化：评估大库滚动、首批缩略图、视频封面和切换恢复，按瓶颈复用缩略图缓存或窗口化渲染。

验收：横竖图、超宽图、低清图、未知尺寸均有测试；过滤和分页相互一致；至少 500 项库做滚动与内存观察；搜索质量有前后对比，不能只靠增加结果数量宣称改善。

### P2-C：生成能力与渠道核实

- 先核对当前安装的 Grok Build、对应源码/工具 schema、官方能力说明和当前账号渠道，记录日期与版本。
- 分别列出普通生图、改图、图生视频的模型、比例、分辨率、时长及调用路径；区分官方 API 的能力与当前订阅工具的能力。
- 已有 API 文档研究不能证明订阅凭证可调用同一能力，也不能证明 API 调用走订阅额度。
- 当前渠道确实支持的选项同时补前端、Host 校验、工具参数和结果验收；不支持的保持明确限制。
- 若必须新增收费 API 渠道，先形成具体接入与费用说明，作为独立决策，不静默改路由。
- 视频 16:9 不通过拉伸原图伪装完成。核实原生行为；需要重构图时，采用明确的先改图再生视频流程并说明额外生成步骤。
- 验证分辨率使用成片实际尺寸，验证时长使用真实媒体信息。缺乏实际模型信息时只记录请求模型，不伪称已确认实际执行模型。

验收：真实支持的选项才可选择，前后端能力一致；代表性真实结果的比例、尺寸和时长匹配；对仅请求审计覆盖、尚未实际生成的组合单独标注。

最新能力报告见 `../qa/2026-09-07-wallpaper-generation-capabilities.md`：安装版为 `1.0.13 (5e9a58528b76)`，检查的本地上游源码为 `9684fa3cdbf2995e30ea8b9b637f1db008f144fc`，两者不是同一构建。源码中的 `image_to_video` 固定模型 `grok-imagine-video-1.5`，只接收图片、提示词、6/10 秒及 480p/720p；带比例的 `reference_to_video` 属于另一工作流。图片模型配置覆盖能力也不等于可选模型或账号权限清单。当前计划不承诺未证实的 1080p、模型选择或订阅额度复用；落实功能前补安装版契约和实际结果证据。

### P3：整体 UI 与 UX 收尾

视觉和交互规范随各阶段落实，本阶段负责统一检查和完整流程验收，不把已知体验问题拖到最后。

| 区域 | 优化内容 | 验收要求 |
| --- | --- | --- |
| 来源与搜索 | 保留七个来源导航；当前来源的搜索、筛选、计数放在一致位置；区分全库搜索与当前已加载结果筛选 | 换来源后条件和结果对应，返回不重复请求，不出现旧来源的错误或计数 |
| 媒体卡片 | 保持原比例和稳定占位；收藏、改图、生成视频使用现有图标库与统一操作位置，次要文件操作收进菜单；键盘聚焦也能触达操作 | 图片不拉伸，追加和重试不重排已有卡片；按钮不遮挡核心预览；视频没有错误的改图入口 |
| 大图与视频预览 | 统一前后切换、原图加载、视频控制和详情；展示真实尺寸、来源和已知生成参数；关闭后回到原卡片 | 点击哪项就打开哪项；失败保留可用缩略图；视频可播放、暂停、拖动进度；预览不意外应用背景 |
| 生成工作区 | 生图、改图、视频使用一致的模式切换和操作区；源图、提示词、可用参数层级清晰；各模式保留自己的输入 | 参数切换实际进入请求；开始、取消、失败重试可用；不自动重试消耗额度；完成后作品可在库中找到 |
| 加载与错误 | 首次加载、追加加载、媒体失败、生成失败、确实无结果分别呈现；保留已有结果；可重试项给出就近操作 | 无无休止占位；局部失败不清空整个画廊；追加失败能重试当前页；错误原因和可用动作相符 |
| 收藏与文件管理 | 单项操作与批量选择分开；选中数量、操作进度、部分失败清晰；删除后能找到恢复入口 | 取消收藏不删文件；操作不静默覆盖；当前背景受保护；批量部分失败可定位并单独处理 |
| 窗口与键盘 | 沿用项目 Select、菜单、弹窗和间距；窄窗口可换行；补齐焦点、图标提示、禁用状态和弹窗焦点恢复 | 主设置与独立主题编辑器均可用；支持 Tab、Enter、Escape；菜单不截断、不透明穿透，中文及英文长文案不重叠 |

测量和记录：至少在应用允许的最小窗口、常用窗口和宽屏窗口检查；覆盖 100% 与 150% 系统缩放、项目现有明暗主题。500 项固定媒体库记录首屏可操作时间、追加页耗时、滚动停顿和往返切换后的内存变化；首次取得基线后再定数值目标，同一设备、样本和缓存条件做前后对比。

验收场景：七个来源各完成一次搜索或读取、预览与返回；代表性图片贯通“找到 → 收藏 → 改图/视频 → 库中再次找到 → 应用”；本地媒体完成另存为、批量部分失败、删除和恢复；离线、慢网、空结果与失效媒体完成错误恢复。两个窗口入口都验收共享行为，不以组件测试代替实机检查。

## 交付节奏与完成标准

| 里程碑 | 包含内容 | 用户可感知结果 |
| --- | --- | --- |
| M1 | P0-A、P0-B | 生图不冒用旧结果、可以取消，完整壁纸库可查可翻 |
| M2 | P1-A、P1-B | 切换不丢现场，加载失败能恢复 |
| M3 | P2-A、P2-B | 收藏和作品可管理，找图筛选更有效 |
| M4 | P2-C 已确认支持的能力 | 生成选项与实际渠道一致；受渠道阻塞项明确列出 |
| M5 | P3 UI/UX 与完整流程验收 | 卡片、预览、生成和管理操作一致，两个窗口入口与窄窗口均可顺畅使用 |

P2-C 的只读能力核实可提前进行，避免最后才发现接入条件；依赖新渠道的工作不能阻塞 M1 至 M3。
各阶段独立交付，不一次堆叠所有功能。开始时标为进行中，通过相应验收后再标为完成。

每个里程碑在 QA 中记录四项：实现范围、自动化检查、对应构建与实机证据、未通过或未覆盖项。现有测试数量仅作为历史记录，不能转写成最新工作树全部通过。M1/M2 优先补齐生命周期边界和实机覆盖；M3 按上面的收藏闭环、单项管理、批量管理、筛选性能分步验收。不按已有按钮数量计算完成度。

每次行为修改都完成交互、视觉和功能验证；新增界面文案同步 15 个语言目录。
不向 `App.tsx` / `AppWorkbench.tsx` 增加领域状态，遵循现有 `Select`、菜单和弹窗规范。
保留当前脏工作树；本次计划修订不提交、推送、发布或重启应用。后续实现阶段按既有授权构建、核对进程并启动新版验收。

测试按改动范围执行相关前端/Rust 回归、类型检查和构建；共享媒体或生命周期变更扩大覆盖到七个来源和两个窗口入口。
每个里程碑实际构建并启动已核对路径和时间的新版程序，完成代表性交互验收，不能用旧可执行文件充当新版本验证。
真实生成控制在必要样本内；其余错误、竞态和分页场景使用 fixture。记录实测与自动化覆盖的区别，不重复运行已经通过且不受新修改影响的检查。

最终完成标准：能从七个来源找图、连续浏览、可靠预览、收藏和重新找到、改图或生成视频、应用背景；任一步失败都不冒充成功、不丢失已有可用结果，参数能力和实际调用一致。
