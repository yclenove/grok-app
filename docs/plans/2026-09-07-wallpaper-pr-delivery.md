# 壁纸开发成果分批 PR 计划

日期：2026-09-07。执行更新：2026-09-08。用户已授权按计划分批推送并创建 PR；未授权合并上游 PR。PR 标题和正文使用中文，标题保留类型/范围前缀，并遵循仓库模板；仓库未强制英文。

## 执行台账（2026-09-08）

- 已从上游 `30757366` 创建 A1、A2、A3 和 B3 独立工作区，原始集成分支及未提交修改保留。
- A1 前端 7,055 项测试、类型检查、lint、UI 构建、质量门禁、网站自测、依赖检查/审计、Rust fmt/clippy 通过；原生测试发现上游设置读写测试隔离缺失，媒体测试通过。
- 新增 A0 `test/isolate-settings-roundtrip`：仅修复设置读写测试的共享目录竞争，优先独立送审；本分支全量验证进行中。
- A2 前端 7,055 项测试及其余前端检查通过；A3 前端 7,058 项测试及其余前端检查通过。原生验证尚待完成，不标为可发布。
- B3 旧代理迁移已提取，尚未完成验证。后续主题继续按下面队列拆分。
- 已创建 [#1074](https://github.com/RongleCat/grok-app/pull/1074)：`test(settings): 隔离设置读写测试的共享目录`，提交 `1e63b928`。本地 7,055 项前端、1,605 项 Rust 测试（1 ignored）以及所有门禁通过，远端 CI 进行中。
- A1 已推送 `89c3b5cf`；包含内部验证前置 #1074，原生 1,607 项通过（1 ignored），fmt/clippy 通过。尚未提 PR，等前置合入后剔除重复差异。
- A2 已推送 `447f863e`，1,608 项 Rust 测试及 fmt/clippy 通过（1 ignored）；A3 已推送 `18ef7d84`，1,605 项 Rust 测试及 fmt/clippy 通过（1 ignored）。各自包含内部验证前置 #1074，尚未提 PR。首批三个修复已完成本地门禁，公开送审等待 #1074 合入后移除重复差异。
- B3 已推送 `88da27ab`；全量前端 7,057 项及其他前端门禁通过，原生验证待办。
- #1074 远端 CI 已全部通过：frontend、Rust macOS、Rust Windows、Rust Linux；PR 仍待维护者审核，未合并。
- 待审上限仍为三个；依赖修复合入前不混入其他独立 PR。仓库模板的中文说明草稿在工作区外 `H:\aicoding\grok-pr-a{0,1,2,3}-body.md`，不随产品 PR 提交。
- 剩余优化已交给独立任务，目录 `H:\aicoding\grok-app-wallpaper-optimization`，分支 `feat/wallpaper-optimization-followup`，与本任务的 PR 拆分隔离。

以下为原始规划记录；“未授权发布”“未新建分支”等历史表述，以本节最新授权和执行状态为准。

### Issue 核对（2026-09-08）

用户要求提交过程中查找相关 Issue。已检索开放及关闭 Issue，覆盖中英文端口、拖放、搜索、代理、设置测试、壁纸、lightbox、Pexels、Openverse 关键词，并读取候选正文/维护者说明。

- A0/A1/A3：当前未检索到直接对应的既有 Issue，不添加自动关闭引用。
- A2：#1017 已关闭，原问题是隐藏窗口期间 OLE 注册失败，上游已通过延迟注册修复。本次是同模块后续资源/状态清理，正文写相关历史，不用 `Fixes`。
- B3：#1046 已关闭，作者确认已有代理功能；不是旧 `use` 值迁移问题，不作为修复关联。
- #618 是第三方视觉输入格式，与本地媒体端口无关；#970 是设置搜索缺少中文关键词，与会话搜索迟到回写无关。
- 壁纸 #927/#934/#941/#846 均为历史外观与模糊/覆盖层问题且已关闭，不能与新增搜索渠道混为一谈。后续各主题公开前继续核对新 Issue。

## 当前结论

已有实现可以开始拆分送审准备，无需等待未来的批量管理、可恢复删除和性能优化全部完成。当前集成分支不适合作为一个 PR 提交。首批先准备三个独立小修复，之后按功能依赖推进；同时待审不超过三个 PR。

本次只核对 Git、上游 PR 状态、源码依赖和现有 QA，并编写本计划。暂停的产品开发没有恢复，也没有新增实机测试。

## 核对基线与排除项

- 当前分支：`fix/wallpaper-responses-network-observability`，已提交 HEAD `19168986`；最近产品提交 `e4e4163d`。
- 本次 fetch 后上游：`RongleCat/grok-app` 的 `main` 为 `30757366a739ec9aaf0ccc95bbb3efe19a067aa9`。
- 共同祖先仍为 `3bee4a884dd1a3150af457ce477fd4692be19822`；上游新增七个提交，本次未合并到开发分支。
- `git diff upstream/main...HEAD`：267 文件，新增 49,604 行、删除 2,594 行；92 个非上游祖先提交含合并记录，不能当作 92 个独立功能。
- 上述体积包括 24 个文档/规则文件、30 个语言文件以及外置和内嵌测试，不等于 49,604 行业务逻辑。Git 原始增删量仍是实际评审成本，不能用“都是测试”忽略。
- 暂停时的三个未提交文件：`WallpaperImagineControls.tsx`、`WallpaperSourceGallery.tsx`、`settings.part4.css`。涉及取消/生成按钮、单图布局和滚动区域；尚未测试、构建，不纳入任何当前候选补丁。

已核对上游合并状态：

| PR | 状态 | 处理 |
| --- | --- | --- |
| [#939](https://github.com/RongleCat/grok-app/pull/939) 本地媒体应用改走有界 IPC | 已合并 | 不重复提交原修复；后续媒体管线新增行为另外审核 |
| [#966](https://github.com/RongleCat/grok-app/pull/966) 主题编辑器窗口死锁 | 已合并 | 不重复提交；独立编辑器缺少预览 Provider 是另一个问题 |
| [#976](https://github.com/RongleCat/grok-app/pull/976) 内置浏览器遮挡弹窗 | 已合并 | 不重复提交 |

`git cherry` 对 #939 对应旧提交仍显示 `+`，不能据此宣称未合并；采用 GitHub 合并状态及当前源码语义核对，避免 squash 历史造成误判。

另有只出现在本地合并提交中的差异，如 Tiptap 版本、CLI update 模块搬迁、session stop 模块搬迁、Review/sessionChanges 和 lobe-turn CSS。它们不是本批壁纸功能的默认组成部分。拆分时保留最新上游实现，逐项确认是否仍有独立价值；不能复制整棵 HEAD 把它们顺带带回去，也不在本次规划中删除这些本地工作。

## 首批：三个独立修复

三个候选均对最新上游临时 Git index 做过 `git apply --cached --check`，补丁可应用；没有修改当前 index 或工作区。**这只证明文本可应用，不证明编译、测试或实际行为通过。** 首批分支仍需按下文门禁重新验证。

### A1：本地媒体端口

- 建议标题：`fix(media): bind loopback server to browser-safe ports`
- 建议分支：`fix/media-browser-safe-ports`
- 范围：`src-tauri/src/media_server.rs`、`docs/llm-wiki/media-delivery.md` 中与端口相关的变更。
- 当前候选差异：2 文件，`+83/-7`（含测试和文档；独立 QA 记录加入后会变化）。
- 原始定位：`b213207a`；以当前审过的最终实现为准，剔除无关空白差异。
- 行为：避免操作系统分配到 Chromium 禁用端口，导致媒体在到达服务前报 `ERR_UNSAFE_PORT`；有界分散尝试，不改变监听地址、路径权限或媒体 token 机制。
- 验收：实际服务端口、文件读取、Range/视频播放、端口候选范围和冲突尝试；确认图片、聊天附件及壁纸预览不回归。
- 依赖：无壁纸新功能依赖，直接基于上游 main。

### A2：Windows 拖拽资源释放

- 建议标题：`fix(windows): release native drop data with COM ownership`
- 建议分支：`fix/windows-drop-medium-ownership`
- 范围：`src-tauri/src/win_file_drop.rs` 与 Cargo 的 `Win32_System_Memory` 功能项，不包含其余新增网络依赖和 `Win32_System_Pipes`。
- 当前主体差异：1 文件，`+164/-43`（含测试）；加最小 Cargo 功能项及验证说明，预计 2–3 文件。
- 原始定位：`c7ef9c2c`；保留上游后来修正的 OLE imports，不直接重放旧版 imports。
- 行为：按 COM 所有权释放 STGMEDIUM，覆盖委托释放及无效 Drop 数据，避免资源释放错误与拖拽状态残留。
- 验收：委托资源恰好释放一次、无效数据清理；Windows 正常图片/多文件拖入及拒绝无效拖入；Rust Windows 测试嵌入仓库清单。
- 依赖：无壁纸新功能依赖，直接基于上游 main。

### A3：搜索面板关闭后的迟到响应

- 建议标题：`fix(search): ignore late palette results after close`
- 建议分支：`fix/search-palette-late-results`
- 范围：`src/hooks/useSearchPalette.ts`、对应测试。
- 当前候选差异：2 文件，`+57/-3`（含测试）。
- 原始定位：`9f1aa261`。
- 行为：关闭面板、清空查询或切换搜索模式后，让已发出的旧请求失效，避免旧结果重新写回。
- 验收：迟到成功/失败/finally、关闭后重新搜索、卸载；普通搜索仍能展示结果。
- 依赖：无。这是整体复核发现的通用搜索修复，单独命名、单独审查，不包装成壁纸搜索功能。

首批送审节奏：先完成 A1 分支及检查，再准备 A2、A3；它们彼此独立，可分别合入，不需要把当前大分支作为前置条件。暂不继续扩展新功能。

## 后续交付队列

以下是代码归属和依赖规划，不是声称这些分支已抽出、可编译或可以立即提交。一个编号代表一个评审主题；超过体积目标时按表内边界再拆，不为固定 PR 数量合并大主题。

| 顺序/主题 | PR 的完整边界 | 依赖与拆分注意 |
| --- | --- | --- |
| B1 预览生命周期与独立编辑器 | ImageViewer/Lightbox 的稳定 Context、关闭与迟到准备失效、视频预览、预览层级；独立编辑器补 Provider | 拆为“预览生命周期”和“视频预览及编辑器接入”两个 PR。上游 ImageViewer 目前没有 `kind` 视频契约，也没有独立 Context 文件，不能直接复制现有编辑器测试后称为独立修复 |
| B2 壁纸组件拆分 | 从现有弹窗提取 Controls/Gallery/Footer、领域 hook，保持上游当前 X/Imagine/库可用 | 独立机械拆分；不同时加入全部七来源按钮、未实现的模式或整页换肤。遵守 App/AppWorkbench 不增长规则 |
| B3 旧代理设置迁移 | `use` 与有效 URL 迁移为 `manual`，前后端一致，写入失败保留原配置 | 参考 `72b95349`；拆出一般设置修复，不带本机端口或任何配置值，也不与相册专用 WebView 路由合并 |
| B4 受控 HTTPS 传输 | safe_https_client、必要 skin_net 适配、代理/DNS/重定向和响应边界、测试 | 接入已有皮肤下载消费者，保持功能完整；仅新增必要 Cargo 依赖。远程媒体读取可作为后续独立 PR 接入，避免一个网络 PR 带入所有 provider |
| C1 现有 X 搜索可靠性 | 共用结果校验/去重；CLI requestId、取消与迟到结果隔离 | 拆成“结果质量”和“任务生命周期”两个 PR，先在现有 CLI 路径实际生效；保留 CLI 默认 |
| C2 Responses 灰度接入 | Host OAuth 读取边界、固定 endpoint/model/tools、设置与注册、可观测回退/熔断 | 依赖 C1。前后端和 15 语言同 PR；首个可用单路实现已经具备取消和凭据隔离。明确兼容接口实验性质，不承诺零账号风险 |
| C3 Responses 多路与追加 | 三路增量返回、部分成功、跨路去重、一次显式加载更多、对应时序测试 | 依赖 C2；使用最终修过的继续搜索和预取错误处理，不先提交已知有 bug 的历史版本。基准脚本/历史完整报告可单列文档测试 PR |
| D1 Openverse | 远程搜索共用协议和 hook、Openverse provider、原图/缩略图、来源及许可、完整 UI | 依赖 B2/B4；只有已接通来源才出现入口，不提前显示 Pexels/Web 占位按钮 |
| D2 Pexels | API key 控件、Host secrets、凭据隔离、认证失败恢复、分页/预取和来源标注 | 依赖 D1 的共用协议；认证缓存隔离与功能同时交付，不留到另一修复 PR；只带测试假凭据 |
| D3 网络搜索 | web_search、页面提取、质量规则、受控远程图加载、网络来源 UI | 依赖 D1、Responses 共用 client；不把网页结果混入 X。先独立交付页面解析/质量模块及行为测试，再交付完整渠道接入，避免一次提交整个 9 千行旧提交 |
| D4 Grok Saved 相册 | 专用 WebView 会话边界、Saved 桥接与媒体读取、入口、状态、缩略图和分页 | 不按 `fe97aeba` 整提交搬入。先拆受控桥接/读取模块（内部契约与测试，无半成品入口），再接完整来源界面。身份隔离、验证页面禁注入、请求取消从第一步保留；相册专用路由在此归属 |
| E1 媒体目录与收藏 | 稳定 ID、旧文件兼容、来源/作者/许可、替换文件身份更新、收藏、远程映射 | 依赖 B2；可先在现有本地库完成记录和收藏，再给各来源补生产者。保留写入失败真实反馈，不把完整错误找回闭环说成已完成 |
| E2 全库分页与浏览恢复 | Host 全集合查询/快照、48 项分页、用途筛选、来源查询和滚动位置恢复 | 依赖 E1；拆“全库分页”和“来源返回恢复”两个 PR。迟到页不能覆盖收藏更新；身份/凭据变化失效不能晚于缓存交付 |
| F1 普通生图可靠性 | 隔离执行目录、精确单次工具审计、取消、结果签名/路径检查、元数据入库 | 依赖 C1 的 runner 与 E1；将 `e4e4163d` 的 tagged progress 兼容一起带入，不能先合已知拒绝有效输出的版本 |
| F2 图生视频 | 源图物化、AVIF/EXIF、独立受限工具、输出检查、本地可编辑提示词、各图入口 | 依赖 B1/B4/E1/F1；上传与 source 转码/取消可先作为已有生成输入通道修复抽出。完整视频 UI 与真实工具参数同批，6/10 秒、480p/720p，不伪造其他能力 |
| F3 改图 | 独立编辑模式、精确提示词/比例传递、父图关联、可恢复错误和元数据 | 依赖 F1/F2 的共用源图处理；不把普通生图、改图和视频一并塞进同一 PR |
| E3 详情与提示词复用 | 作品详情、父图导航、顶层 Escape、提示词仅预填和模式草稿隔离 | 依赖 E1 与对应生成生产者；`b7349c92` 的关闭后父图预览拒收同批纳入 |

同一源码文件可能在不同 PR 中逐步演进，尤其 `wallpaper_source.rs`、`WallpaperSourceModal.tsx`、`api/wallpaper.ts`、`lib.rs`、Cargo 和语言目录。应按行为拆补丁，不能简单按目录把“全部后端”和“全部前端”分两次上。

主题/皮肤 Provider 的 HMR 修复（`570cb737`、`a0f8ea01`）、主题菜单点击修复（`cb55c73a`）、CI PATH/行尾测试兼容等可另列小修复；提取时先确认最新上游是否仍需要。它们不自动塞进 B1 或其他功能 PR，也不要求全部先合完才能开始壁纸主线。

## 体积和分支规则

1. Bugfix 尽量控制在 2–10 个主要文件；功能 PR 的人工审查实现目标为约 500–1,500 行。单独列出测试、机械移动、15 语言和锁文件增量，同时报告原始总 diff；它们不受业务行数目标掩盖。
2. 若仍超过约 1,500 行实现，先找可以在现有功能中独立生效的前置改动；必要的内部协议/解析器可先交付测试和调用契约，但不暴露不能工作的按钮，也不删测试或翻译来压体积。网络和身份边界必须作为完整单元审核。
3. 保留当前集成分支及三个未提交文件。执行拆分时在单独 worktree 从最新上游新建干净分支；本次没有新建。
4. 原提交只作为追溯索引。特别是 `fe97aeba`、`159f5a46`、`46423e4b`、`6633e1b8` 同时包含多项能力，不能直接 cherry-pick 作为一个 PR。按最终修正后的逻辑重新组织提交，并保留贡献署名。
5. 首批三个分支均直接针对上游 main。后续依赖功能优先等前置合入后再开下一个 PR，避免十几个堆叠 PR 和重复 diff；若内部需要堆叠准备，公开前重建到已合入的主干并重跑受影响验证。
6. 每个 PR 的代码、对应测试、15 语言和当前行为文档一起提交。长期开发计划和逐日实验日志不反复复制到每个 PR，改为简短的该批验证记录；保留本地完整交接材料。
7. 发布前核对 `base...head` 的完整 diff，不携带其他来源入口、已合入修复、旧上游代码或无关格式化；不直接从旧 HEAD 覆盖共享文件。

## 每个 PR 的准备完成条件

- 对准该 PR 的实际基底复现问题或说明新增行为，完成必要的前端/Rust 回归，再按 `docs/llm-wiki/maintain.md` 与 CI 执行 typecheck、test、lint、质量门禁、网站脚本 self-test、UI build、Rust fmt/clippy/test。
- Windows 测试 harness 使用仓库 CI 的 Common Controls manifest 方法；不能把 `0xc0000139` 当作断言失败或跳过结果。
- 记录该分支 commit、构建时间和对应实机范围。当前集成分支曾通过 7,366 项前端、1,831 项 Rust 测试（另 1 ignored），只是历史基线，不等于新拆出的分支通过。
- UI 变更检查普通设置和独立编辑器入口、明暗/窄窗口、加载/失败/焦点；按实际覆盖写报告，不把全部七来源都标成已验收。
- PR 描述遵循仓库模板，讲清问题、用户可见变化、依赖、验证和剩余边界；标题使用 `fix/feat/refactor(scope): ...`。每个 PR 可独立审查、回滚，且没有已知必须依靠下一个 PR 才能工作的公开入口。
- 通过上述检查后再进入用户审阅的候选清单。本次规划不视为推送、公开 PR 或合并授权。

## 继续留在优化计划的工作

未验证的取消按钮和布局改动、视频实际尺寸/时长自动入库、目录保存失败找回、可恢复删除/恢复、另存为/打开文件夹、批量操作、更多筛选/确定性去重、500 项性能与完整质量对照，仍按 [模块优化计划](2026-09-06-wallpaper-module-optimization.md) 后续实施。不能在本次 PR 描述中列为已完成，也不以它们尚未完成为理由阻止已独立验证的小修复送审。

本轮产物仅为本计划。接下来建议先执行 A1 的独立分支提取与验证，再依次 A2、A3，形成首批可审核报告后再安排发布。
